<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间：mapper接口全名 -->
<mapper namespace="org.meetu.dao.IUserDao">

	<sql id="userFields"> 
		id,mobile,pwd,imei,name,nickname,birthdate,gender,qq,wechat,email,company,company_addr,home_addr,regtime,mood,status,imgUrl,imgUrlHD
    </sql>

	<!-- 主键查询 -->
	<!-- resultType,parameterType可以写别名alias也可以直接写类全名 -->
	<select id="queryById" parameterType="java.lang.String" resultType="org.meetu.model.User">
		select <include refid="userFields"/> from _user where id = #{id};
	</select>
	
	<select id="queryById_Map" parameterType="java.lang.String" resultType="map">
		select * from _user where id = #{id};
	</select>
	
	<insert id="insert" parameterType="User">
		insert into u_user ( <include refid="userFields"/> ) values
		(
			#{id} , #{mobile}, #{pwd}, #{imei}, #{name} , #{nickname} , #{birthdate} , #{gender} , #{qq} , #{wechat} , #{email} , #{company} , #{company_addr} , #{home_addr} , #{regtime} , #{mood} , #{status} , #{imgUrl} , #{imgUrlHD}
		);
	</insert>
	
	<select id="queryFewTable"></select>
	
	
</mapper>